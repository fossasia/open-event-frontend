<div class="ui list basic segment m-0 pb-0">
  <div class="item">
    <i class="clock alternate outline icon pt-1"></i>
    <div class="content">
      {{#if this.isSingleDay}}
        {{general-date @event.startsAt "dddd, D MMMM, YYYY" tz=@event.timezone}}<br>{{general-date @event.startsAt "h:mm A" tz=@event.timezone}} {{t 'to'}} {{general-date @event.endsAt "h:mm A" tz=@event.timezone}} 
      {{else}}
        {{general-date @event.startsAt ft='date-time-long' tz=@event.timezone}} {{t 'to'}} {{general-date @event.endsAt ft='date-time-long' tz=@event.timezone}} 
      {{/if}}
      ({{this.timezone}})
      <br>
      <UiDropdown class="ui floating dropdown button mt-4">
        {{t 'Add to Calendar'}}
        <div class="menu">
          {{#each this.calendarUrls as |calendar|}}
            <a href={{calendar.url}} class="item" target="_blank" rel="noreferrer nofollow">
              {{calendar.name}}
            </a>
          {{/each}}
        </div>
      </UiDropdown>
    </div>
  </div>
</div>
