<div class="ui basic {{if this.isLoading 'loading'}} segment">
  <Forms::Admin::Settings::Billing @timezones={{this.timezones}} @countries={{this.countries}} @model={{this.model}} @save={{action "updateInvoiceModel"}} />
</div>
