<div class="ui container">
  <h1 class="ui header">{{t 'My Sessions'}}</h1>
  <div class="ui grid">
    <div class="row">
      <div class="twelve wide column">
        {{#tabbed-navigation}}
          {{#link-to 'admin.users.view.sessions.list' 'upcoming' class='item'}}
            {{t 'Upcoming Sessions'}}
          {{/link-to}}
          {{#link-to 'admin.users.view.sessions.list' 'past' class='item'}}
            {{t 'Past Sessions'}}
          {{/link-to}}
        {{/tabbed-navigation}}
      </div>
    </div>
    {{outlet}}
  </div>
</div>
