<div class="ui grid">
  <div class = "row">
    <div class = "sixteen wide column">
      <h1>
        {{t 'My tickets'}}
      </h1>
      <div class="ui fluid pointing secondary menu">
        {{#link-to 'my-tickets.index' class='item'}}
          {{t 'Upcoming Events'}}
        {{/link-to}}
        {{#link-to 'my-tickets.saved' class='item'}}
          {{t 'Saved Events'}}
        {{/link-to}}
        {{#link-to 'my-tickets.past' class='item'}}
          {{t 'Past Events'}}
        {{/link-to}}
      </div>
    </div>
  </div>
  {{outlet}}
</div>
