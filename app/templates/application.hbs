<div class="pusher">
  {{nav-bar}}
  {{#if (eq routing.currentRouteName 'index')}}
    {{welcome-header}}
  {{/if}}
  <div class="ui container main-container">
    {{outlet}}
  </div>
  {{footer-main}}
</div>