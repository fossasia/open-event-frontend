{{#if extraRecords.isLocked}}
  <UiPopup @content={{t "Unlock Session"}} @class="ui basic button" @click={{action props.actions.unlockSession record}} @position="left center">
    <i class="lock icon"></i>		
  </UiPopup>
{{else}}
  <UiPopup @content={{t "Lock Session"}} @class="ui basic button" @click={{action props.actions.lockSession record}} @position="left center">
    <i class="unlock icon"></i>		
  </UiPopup>
{{/if}}