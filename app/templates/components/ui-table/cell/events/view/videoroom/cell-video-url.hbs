{{#if @record.url}}
  <div class="truncate">
    {{#if (eq @record.videoChannel.provider 'bbb')}}
      {{t 'Auto-Generated'}}
    {{else}}
      <a class="link-item" href="{{@record.url}}">{{@record.url}}</a>
    {{/if}}
  </div>
{{/if}}
