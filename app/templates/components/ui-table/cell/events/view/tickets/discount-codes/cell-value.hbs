<span>
  {{#if (eq this.extraRecords.type 'percent')}}
    {{record}}{{t '%'}}
  {{else if (eq this.extraRecords.type 'amount')}}
    {{t this.currency-symbol this.extraRecords.event.paymentCurrency }}{{record}}
  {{/if}}
</span>
