<div class="ui segments">
  {{#if showBanner}}
    <div class="ui orange inverted segment center aligned">
      <div class="ui inverted mini statistic horizontal">
        <div class="value">
          {{t 'Event Information'}}
        </div>
      </div>
    </div>
  {{/if}}
  <div class="ui secondary segment">
    <h3 class="weight-400">{{t 'When & Where'}}</h3>
  </div>
  <div class="ui padded segment">
    <strong>{{t 'At'}} {{data.event.locationName}}</strong>
    <br>
    <strong>{{t 'From'}}:</strong> {{general-date data.event.startsAt data.event.timezone}}
    <br>
    <strong>{{t 'To'}}:</strong> {{general-date data.event.endsAt data.event.timezone}}
    {{#if data.event.ownerName}}
      <br>
      <strong>{{t 'Organized By'}}:</strong> {{data.event.ownerName}}
    {{/if}}
  </div>
  <div class="ui padded segment">
    <a href="{{data.event.url}}">{{t 'Go to event'}}</a>
  </div>
</div>
