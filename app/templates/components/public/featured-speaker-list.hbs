<h3>{{t 'Featured Speakers'}}</h3>
<div class="speaker-list ui three column grid">
  {{#each this.speakers as |speaker|}}
    <Public::SpeakerItem @speaker={{speaker}} @timezone={{@timezone}} @class="column" @isFeatured={{true}} />
  {{/each}}
</div>
