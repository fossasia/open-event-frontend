{{#if this.icon}}
  <i class="time icon"></i>
{{/if}}
<div class="ui input">
  <Input @type="text" @value={{value}} placeholder={{this.placeholder}} @name={{name}} @focus-out={{action "onChange"}} />
</div>
