<div class="ui grid">
  <div class="row">
    <div class="{{if this.device.isMobile 'sixteen' 'three'}} wide column">
      <TabbedNavigation @isVertical={{true}}>
        <LinkTo @route="events.view.team.index" class="item">
          {{t 'Team Members'}}
        </LinkTo>
        <LinkTo @route="events.view.team.permissions" class="item">
          {{t 'Permissions'}}
        </LinkTo>
      </TabbedNavigation>
    </div>
    <div class="{{if this.device.isMobile 'sixteen' 'thirteen'}} wide column">
      {{outlet}}
    </div>
  </div>
</div>
