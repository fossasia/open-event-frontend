<table class="ui very basic unstackable table mt-4">
  <thead>
    <tr>
      <th>{{t 'User (Email)'}}</th>
      <th>{{t 'Role'}}</th>
      {{#if @editable}}
        <th>{{t 'Options'}}</th>
      {{/if}}
    </tr>
  </thead>
  <tbody>
    {{#each this.model.group.roles as |roleDetail|}}
      <tr>
        <td>{{roleDetail.user.email}}</td>
        <td>{{roleDetail.role.titleName}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>
