<div class="ui segment">
  <div class="ui grid">
    <div class="sixteen wide column">
      <h1 class="ui header">{{model.name}}</h1>
      <p>{{t 'Page Description: '}}{{sanitize model.description}}</p>
    </div>
  </div>
</div>
