<div class="ui segment">
  <div class="ui grid">
    <div class="sixteen wide column">
      <h1 class="ui header">{{this.model.name}}</h1>
      <div>{{sanitize this.model.description}}</div>
    </div>
  </div>
</div>
