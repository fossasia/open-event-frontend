<div class="ui container">
  <div class="column sixteen wide">
    <h2 class="ui header center aligned">
      {{t 'Edit Session'}}
    </h2>
    <div class="ui container">
      <Forms::SessionSpeakerForm @fields={{model.forms}} @data={{model}} @isLoading={{isLoading}} @save={{action "save"}} @isSession={{true}} @includeSession={{true}} @isSessionSpeaker={{true}} />
    </div>
  </div>
</div>
